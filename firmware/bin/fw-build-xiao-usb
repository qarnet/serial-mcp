#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=firmware/bin/fw-common.sh
source "$SCRIPT_DIR/fw-common.sh"

cd "$(_fw_repo_dir)"
exec west build -b xiao_ble firmware/ --pristine -- \
  -DEXTRA_CONF_FILE=boards/xiao_ble_usb.conf \
  -DEXTRA_DTC_OVERLAY_FILE=boards/xiao_ble_usb.overlay \
  "$@"
