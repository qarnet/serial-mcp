#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
FIRMWARE_DIR="$(dirname -- "$SCRIPT_DIR")"
REPO_DIR="$(dirname -- "$FIRMWARE_DIR")"

cd "$REPO_DIR"
exec west build -b native_sim firmware/ "$@"
