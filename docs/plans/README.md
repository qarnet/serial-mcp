# Serial MCP Work Plans

The phased RX redesign plans have been completed.

Remaining active work now lives in:

- `CLEANUP.md` - remove drift, fix subscribe accounting, remove deprecated APIs, and align docs/tests with the final RX model

Backlog-only ideas stay in `FEATURES.md` and are not part of the active cleanup plan.
