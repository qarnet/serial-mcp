* BACKLOG [#2] PB-024 Filtering/search across captures
  :PROPERTIES:
  :ID:         PB-024
  :TYPE:       feature
  :SIZE:       M
  :AREA:       capture
  :DEPENDS_ON:

** Problem

Finding specific events in captures requires external tools.

** Desired outcome

Search across captures if it includes direction, timestamps, event
types, parsed fields.

** Scope

- Search over capture data with structured filters.

** Non-goals

- Free-text-only search (unclear value; an LLM can use grep).

** Acceptance criteria

- [ ] Filters include direction, timestamps, event types, parsed fields.

** Technical context

Worth it only with the structured filters above.

** Open questions

Concrete usage evidence needed before READY.

** Implementation plan

Filled when implementation begins.

** Implementation notes

Updated while implementation is active.

** Result

Filled before the item enters REVIEW.
