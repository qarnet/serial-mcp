* BACKLOG [#2] PB-014 Flow-control-aware ring backpressure (pause-on-full)
  :PROPERTIES:
  :ID:         PB-014
  :TYPE:       feature
  :SIZE:       M
  :AREA:       rx
  :DEPENDS_ON:

** Problem

The RX ring wraps when full, silently dropping oldest bytes; hardware
RTS/CTS backpressure semantics are not restored to the sender.

** Desired outcome

`on_full: "wrap" | "pause"` with observable paused-state events,
restoring hardware flow-control backpressure semantics.

** Scope

- Pause-on-full ring mode.
- Observable paused-state events.

** Non-goals

- Per-client cursors (PB-016).

** Acceptance criteria

- [ ] Pause mode stops consuming without data loss while paused.
- [ ] Paused/resumed states are observable through the public surface.

** Technical context

src/rx_ring.rs, src/rx_session.rs.

** Open questions

None.

** Implementation plan

Filled when implementation begins.

** Implementation notes

Updated while implementation is active.

** Result

Filled before the item enters REVIEW.
