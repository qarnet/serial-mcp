{
  "$comment": "Save as .vscode/mcp.json in your workspace root (VS Code Copilot MCP support).",
  "servers": {
    "serial": {
      "type": "stdio",
      "command": "serial-mcp",
      "args": ["--allowlist=/dev/ttyACM*,/dev/ttyUSB*"]
    }
  }
}
