{
  "name": "mcp-validation",
  "version": "0.0.0",
  "private": true,
  "description": "Lockfile-only MCP validation tools: official conformance and Inspector. Install with `npm ci --ignore-scripts`; lifecycle scripts never run and packages never resolve dynamically via npx.",
  "dependencies": {
    "@modelcontextprotocol/conformance": "0.2.0-alpha.10",
    "@modelcontextprotocol/inspector": "2.0.0"
  },
  "overrides": {
    "ink": "6.8.0",
    "ink-form": {
      "ink-select-input": "6.2.0"
    }
  }
}
