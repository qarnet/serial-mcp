# Expected failures for pinned @modelcontextprotocol/conformance@0.2.0-alpha.10.
#
# Four checks depend on fixture endpoints or capabilities that serial-mcp does
# not implement: task/elicitation fixtures, capability spoofing,
# independent-request streaming beyond the MCP contract, and MCP logging.
# These entries document known fixture gaps; they do not broaden acceptance.
# A passing entry makes the baseline stale and fails the run. Any other failure
# is an unexpected regression.
#
# Use the runner's `<scenario>:<check-id>` per-check baseline. The check ID is
# the left-hand column printed by `conformance server`. Do not rename IDs or
# change scope; only representation may adapt to the pinned runner.
server:
  - server-stateless:sep-2575-server-rejects-undeclared-capability
  - server-stateless:sep-2575-missing-capability-http-400
  - server-stateless:sep-2575-http-server-no-independent-requests-on-stream
  - server-stateless:sep-2575-server-no-log-without-loglevel
