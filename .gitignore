/result
/.direnv/
/target/
/build/
/xtask/target/
/xtask/Cargo.lock
tests/proptest.proptest-regressions
fuzz/target/
fuzz/Cargo.lock
STATUS.md
.idea/
.vscode/
.schemas/
.envrc
mutants.out/
mutants.out.old/
logs
# npm install output for the lockfile-pinned MCP validation project
# (compat/mcp-validation): only package.json + package-lock.json are committed;
# the node_modules tree is regenerated by `npm ci --ignore-scripts`.
compat/mcp-validation/node_modules/
# local symlink to protocol reference material (specs, RFCs); not repo content
/resources
__pycache__/
