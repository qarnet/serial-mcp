{
  "$comment": "Drop-in MCP server configuration for Claude Desktop. Place in claude_desktop_config.json.",
  "mcpServers": {
    "serial": {
      "type": "stdio",
      "command": "serial-mcp",
      "args": ["--allowlist=/dev/ttyACM0"]
    }
  }
}
