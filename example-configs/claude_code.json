{
  "$comment": "Add to .mcp.json (project) or ~/.claude.json (global). See docs/agent-config.md for details.",
  "mcpServers": {
    "serial": {
      "type": "stdio",
      "command": "serial-mcp"
    }
  }
}
